# 灵感记录卡 - 需求规格文档

> 版本：v1.0  
> 更新日期：2026-08-16

---

## 一、项目概述

一款轻量级的本地灵感记录工具，以「黑底 + 黑客数据流」为视觉基调，支持灵感的增删改查、标签化管理、多主题切换、卡片动画展示、本地导入导出等功能。纯前端实现，**无需后端、无需构建工具**，双击 `index.html` 即可运行。

---

## 二、技术栈

| 类别 | 选型 | 说明 |
|------|------|------|
| 语言 | 原生 HTML + CSS + JavaScript (ES6+) | 无框架依赖，开箱即用 |
| 存储 | 浏览器 localStorage（优先） / IndexedDB（备选，如数据量过大） | 数据完全本地存储，不上传任何服务器 |
| 数据格式 | JSON | 导入导出统一使用 JSON |
| 兼容 | Chrome / Edge / Firefox / Safari 近两年版本 | 无需兼容 IE |

---

## 三、核心功能清单

### 3.1 灵感卡片基础操作

| 功能 | 详细说明 |
|------|----------|
| 新增灵感 | 点击「+ 新建」按钮，弹出编辑弹窗，填写后保存即生成卡片 |
| 编辑灵感 | 点击卡片弹出编辑弹窗，修改内容后保存，更新修改时间 |
| 删除灵感 | 编辑弹窗中含「删除」按钮，点击后二次确认（「确定删除这条灵感？」）再删除；卡片右下角也可直接放置删除图标按钮 |
| 搜索灵感 | 顶部搜索框，输入关键词实时过滤标题和内容，支持模糊匹配 |
| 标签筛选 | 点击任一标签（卡片上的标签、标签云/标签栏中的标签），仅展示含有该标签的卡片；再次点击取消筛选 |
| 排序切换 | 顶部排序下拉：「按修改时间 ↓」（默认）、「按修改时间 ↑」、「按创建时间 ↓」、「按创建时间 ↑」 |

### 3.2 标签系统

- 每条灵感可挂 **0~N 个标签**，标签由用户完全自由输入
- 输入标签时支持 **回车确认**、**逗号分隔** 自动拆分
- 重复标签自动去重
- 顶部/侧边显示「标签云」区域，汇总所有已使用的标签，按使用次数排序；点击即筛选
- 标签可在编辑弹窗中单独增删

### 3.3 多主题配色（4 套数据流主题）

取消日夜间模式，改为 4 套暗色数据流配色一键切换，切换即时生效并持久化保存：

| 主题名 | 主色调 | 背景 | 文字色 | 边框/强调色 |
|--------|--------|------|--------|-------------|
| 经典黑客绿（默认） | `#00ff41` | 深黑 `#0a0a0a` | 浅绿灰 | 荧光绿 |
| 赛博蓝 | `#00d4ff` | 深蓝黑 `#070f1a` | 浅蓝灰 | 荧光蓝 |
| 暗紫 | `#c77dff` | 深紫黑 `#110819` | 浅紫灰 | 荧光紫 |
| 琥珀橙 | `#ffb703` | 深橙黑 `#1a1208` | 浅橙灰 | 荧光橙 |

> 每套主题的背景上均叠有对应色调的「数字雨」动画 + 半透明遮罩层，确保文字清晰可读。

### 3.4 字体与卡片配色（预设选项）

**字体预设（全局切换，持久化保存）：**
1. 等宽字体（`Consolas / Monaco / 'Courier New'`，黑客风格默认）
2. 无衬线黑体（`'Microsoft YaHei' / 'PingFang SC' / sans-serif`）
3. 衬线宋体（`'SimSun' / 'Songti SC' / serif`）
4. 楷体（`'KaiTi' / 'STKaiti' / serif`）

**卡片配色预设（每张卡片单独选择）：**
提供 6 种半透明卡片配色方案，用户在编辑弹窗中为每张卡片挑选：
1. 主题色半透明（与当前全局主题同色，默认）
2. 纯黑低透明
3. 深灰半透明
4. 矩阵绿（固定绿色，不受全局主题影响）
5. 深靛蓝
6. 暗酒红

### 3.5 卡片展示与动画

#### 布局：响应式瀑布流
- 大屏（≥1400px）：4 列
- 中屏（≥1024px）：3 列
- 小屏（≥640px）：2 列
- 手机（<640px）：1 列
- 卡片宽度固定，高度随内容自适应，按列填充（Pinterest 风格）
- 卡片最大高度限制（如 500px），超出内容显示省略号，点击进入弹窗查看完整内容

#### 动画效果

| 场景 | 动画描述 |
|------|----------|
| **卡片较少时（< 一屏）** | 每张卡片以不同节奏、不同相位 **缓慢上下浮动 4~8px**，周期 3~5 秒；使用 CSS `transform: translateY()` + `animation`，性能开销低 |
| **卡片满屏 / 滚动时** | 取消浮动动画，改为滚动时卡片正常跟随；新卡片入场时有轻微淡入 + 上移效果（`opacity` + `translateY`） |
| **删除卡片** | 卡片先缩小淡出，其余卡片平滑补位（transition） |
| **数字雨背景** | Canvas 实现，字符（数字 0-9、少量字母和符号）从上往下流动，透明度控制在 15% 以内；叠加 `rgba(0,0,0,0.75)` 以上的遮罩层，确保前景文字对比度足够 |
| **Hover 效果** | 鼠标悬停卡片时轻微上浮（`translateY(-3px)`）+ 外发光（`box-shadow` 使用主题色），过渡 0.25s |

### 3.6 本地导入 / 导出

| 功能 | 说明 |
|------|------|
| **导出** | 点击「导出」按钮，触发浏览器下载，文件名 `灵感备份_YYYYMMDD_HHMMSS.json`，内容为完整数据 JSON（含所有卡片、标签、配置项） |
| **导入** | 点击「导入」按钮 → 弹出文件选择框 → 选择 `.json` 文件 → 解析后弹出预览（显示将导入 X 条灵感，是否覆盖现有数据 / 合并追加）→ 用户确认后执行导入 |
| **冲突处理** | 导入提供两种模式：① **覆盖模式**（清空现有，替换为导入数据）；② **合并模式**（追加到现有，按唯一 ID 去重） |

### 3.7 首次启动引导（分步弹窗）

用户首次打开（`localStorage` 中无 `hasShownGuide=true` 标记）时，展示 **5 步蒙层引导**，每步均有「跳过」「上一步」「下一步」按钮，最后一步为「立即开始」：

| 步骤 | 标题 | 内容说明 | 高亮区域 |
|------|------|----------|----------|
| 1/5 | 欢迎使用灵感记录卡 👋 | 这是一款完全本地化的灵感记录工具，你的想法只会保存在当前浏览器中。 | 全屏蒙层无高亮 |
| 2/5 | 记录你的每一个灵感 | 点击右上角「+ 新建」按钮，即可创建一张新的灵感卡片，随时捕捉创意。 | 高亮「+ 新建」按钮 |
| 3/5 | 标签化管理 | 为灵感添加标签，点击标签即可快速筛选同类内容；也可以用搜索框查找关键词。 | 高亮标签云区域 + 搜索框 |
| 4/5 | 个性化外观 | 在这里切换数据流主题配色、修改阅读字体，打造你的专属黑客控制台风格。 | 高亮主题切换 + 字体选择区域 |
| 5/5 | 数据安全与备份 | 所有数据均存储在本地浏览器。记得定期使用「导出」功能备份 JSON 文件，也可以从其他设备导入。 | 高亮导入 / 导出按钮 |

> 引导结束后写入 `hasShownGuide=true`；设置中提供「重新查看引导」入口。

---

## 四、数据结构设计

### 4.1 单条灵感（Card）

```json
{
  "id": "uuid_xxxxxxxxxxxx",
  "title": "灵感标题（可选，可空）",
  "content": "灵感正文内容",
  "tags": ["标签1", "标签2"],
  "cardColor": "theme_default",
  "createdAt": 1723785600000,
  "updatedAt": 1723785600000
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | UUID，唯一标识，用于导入去重 |
| title | string | 标题，可空 |
| content | string | 正文，必填 |
| tags | string[] | 标签数组 |
| cardColor | string | 卡片配色预设 key，如 `theme_default` / `pure_black` / `matrix_green` 等 |
| createdAt | number | 创建时间戳（毫秒） |
| updatedAt | number | 最后修改时间戳（毫秒） |

### 4.2 全局配置（Config）

```json
{
  "theme": "green",
  "fontFamily": "mono",
  "sortBy": "updatedAt_desc",
  "hasShownGuide": true
}
```

### 4.3 导出的完整 JSON 结构

```json
{
  "version": "1.0",
  "exportedAt": 1723785600000,
  "config": { /* 见上 */ },
  "cards": [
    /* 卡片数组 */
  ]
}
```

---

## 五、页面结构（组件拆分思路）

单页面应用，原生 JS 以「模块」方式组织（一个 IIFE 或简单的模块对象），主要包含以下区域：

```
┌─────────────────────────────────────────────────────────────┐
│  <顶部导航栏 TopBar>                                         │
│  [Logo] [搜索框] [排序下拉] [标签筛选] [主题切换] [字体切换]  │
│                                        [导入] [导出] [+新建] │
├─────────────────────────────────────────────────────────────┤
│  <可选：标签云 TagCloud>                                     │
│   #产品 #技术 #随笔 #读书 ... (按使用次数排序)                │
├─────────────────────────────────────────────────────────────┤
│  <卡片瀑布流 Waterfall>                                      │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐                           │
│  │卡片1│ │卡片2│ │卡片3│ │卡片4│  ... (响应式列数)           │
│  └─────┘ └─────┘ └─────┘ └─────┘                           │
│  ┌─────┐ ┌─────┐ ...                                        │
│  │卡片5│ │卡片6│                                            │
│  └─────┘ └─────┘                                            │
├─────────────────────────────────────────────────────────────┤
│  <空状态 EmptyState>（无卡片时显示）                         │
│  "还没有灵感？点击右上角「+ 新建」开始记录你的第一个想法吧"    │
└─────────────────────────────────────────────────────────────┘

<叠层元素>
  ├── 数字雨背景 Canvas（z-index: -1，固定全屏）
  ├── 编辑弹窗 EditModal
  ├── 删除确认弹窗 ConfirmModal
  ├── 导入预览弹窗 ImportModal
  └── 首次引导蒙层 GuideOverlay
```

---

## 六、视觉与交互规范

### 6.1 整体风格关键词
黑客帝国 / 数据流 / 暗色控制台 / 荧光发光边框 / 等宽字体默认

### 6.2 文字可读性保障（**强约束，不得破坏**）
- 正文文字与背景对比度 ≥ **WCAG AA 级标准（4.5:1）**
- 数字雨背景透明度 ≤ **15%**，且叠加不低于 `rgba(0,0,0,0.75)` 的遮罩层
- 卡片内部背景独立，不直接叠加在数字雨上
- 发光效果（`text-shadow` / `box-shadow`）仅用于边框、Hover 等装饰，**不作用于正文文字**

### 6.3 圆角、间距、阴影
- 卡片圆角：`8px`
- 弹窗圆角：`12px`
- 按钮圆角：`6px`
- 内边距基础单位：`12px / 16px / 24px`
- 卡片外阴影：`0 2px 12px rgba(主题色, 0.15)`
- 弹窗外阴影：`0 8px 32px rgba(主题色, 0.25)`

### 6.4 操作反馈
- 所有可点击元素 Hover 时改变光标 + 视觉反馈（颜色/发光/位移）
- 保存、删除、导入等操作完成后，右上角弹出 2 秒 Toast 提示（如「已保存 ✓」「已删除」「导入成功，共 X 条」）
- 异步操作（如解析大的导入文件）时显示 Loading 遮罩

---

## 七、文件结构（计划）

```
cards/
├── index.html              # 主页面，所有结构
├── css/
│   ├── style.css           # 主样式 + 布局
│   ├── themes.css          # 4 套主题 CSS 变量定义
│   └── animations.css      # 数字雨、浮动、淡入等动画样式
├── js/
│   ├── app.js              # 入口、初始化、事件绑定
│   ├── storage.js          # localStorage 读写、导入导出
│   ├── cards.js            # 卡片 CRUD、瀑布流渲染、排序筛选搜索
│   ├── editor.js           # 编辑弹窗、标签输入
│   ├── theme.js            # 主题切换、字体切换
│   ├── guide.js            # 首次启动分步引导
│   ├── matrix-bg.js        # Canvas 数字雨背景动画
│   └── utils.js            # 工具函数：UUID、时间格式化、防抖节流等
└── 灵感记录卡-需求规格文档.md  # 本文档
```

---

## 八、验收标准（Definition of Done）

1. ✅ 双击 `index.html` 可直接打开运行，无控制台报错
2. ✅ 灵感卡片可以新增、编辑、删除，刷新页面后数据不丢失
3. ✅ 标签可自由添加、点击标签可筛选、搜索框可实时过滤
4. ✅ 4 套主题配色切换即时生效，刷新后保留选择
5. ✅ 4 种字体预设切换即时生效，刷新后保留选择
6. ✅ 卡片数量少时可见缓慢浮动动画；滚动时动画不卡顿
7. ✅ 导出可下载 JSON 文件，导入可选择覆盖/合并，导入后数据正确显示
8. ✅ 首次访问弹出 5 步引导，可跳过、可前后翻页，完成后不再自动弹出
9. ✅ 所有文字在任意主题下清晰可读，数字雨不干扰阅读
10. ✅ 响应式：窗口缩放到手机宽度时变为单列，布局不错乱

---

## 九、后续可扩展项（不在本次 v1.0 范围内）

- [ ] Markdown 语法渲染支持
- [ ] 卡片内插入图片（base64 存储）
- [ ] 导出为 Markdown / TXT 格式
- [ ] 回收站（误删恢复）
- [ ] 卡片置顶功能
- [ ] 密码加密本地存储
- [ ] 云同步（WebDAV / Gist 等）

---

**—— 以上为本项目 v1.0 完整需求规格 ——**
